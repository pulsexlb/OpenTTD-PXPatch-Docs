# 简单的列车对接
## 列车对接调度计划介绍
列车使用两种不同的调度计划实现列车对接操作，分别为`等待连接`和`前往连接`，可在`前往`下拉菜单中选择并添加这两种调度计划。

![](/images/couple-orders.png)

对接操作只能发生在车站内。

### 等待连接
如果一个列车调度计划为等待连接，列车将在原地等待其他列车进行对接。

![](/images/wait-couple.png)

此调度计划只在当列车位于站台中时有效，当列车在站台外等待对接，或列车长度超过站台时，将提示`无法在站外等待对接`

![](/images/cant-couple-outside-st.png)

### 前往连接
当一个列车调度计划为前往连接时，它将默认在**全图范围内**搜索所有等待对接的列车。在找到目标后，列车将前往等待对接的列车的位置，并减速进行对接。

你也可以为前往对接调度计划添加限制，限制其对接指定列车。另见[列车对接限制](./2.2-列车对接操作/#列车对接限制)

**注意：如果一列列车无法找到任何一列符合要求的等待对接的列车，该列车将被设置为`stuck`状态，简单来说，列车将被会任何一个信号灯阻塞，直到出现一列符合要求的列车**

## 列车对接要求
为了确保解挂后的列车行为正常，以下情况游戏不会执行对接操作：
1. 对接后的列车不符合 start stop（常在 newgrf 中使用）

目前，对接失败时，游戏不会给出任何提示。

**注：双头机车对接后，可能会反转部分车厢顺序，这与对接方向有关，且仅会在双头机车上出现**
