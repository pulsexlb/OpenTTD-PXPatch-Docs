# 自定义调度计划表
在原版 OpenTTD 中，调度计划表总是是与通过载具绑定的，一列载具只能拥有一个调度计划表，多列载具可能共享同一调度计划表。在 px-patch 中，玩家可以在不创建载具的情况下创建调度计划表，并在载具中使用调度计划表

## 调度计划表列表窗口
在地图的下拉菜单中，可打开调度计划表列表窗口：

![](/images/orderlist-manager.png)

在调度计划表列表窗口中，可以创建、删除、重命名调度计划表，或将调度计划表设置为公开可见。

**⚠️注意：当一个调度计划表被设为公开可见后，无法再将其设为私有**

**⚠️注意：当一个调度计划表正在被载具使用时，无法删除**

