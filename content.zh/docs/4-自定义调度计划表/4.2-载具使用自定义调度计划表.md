# 载具使用自定义调度计划表
## 直接在调度计划中使用
在载具的调度计划中，可直接选择“执行调度计划”并选择一个自定义调度计划表。当执行到这个调度计划表时，列车的调度计划将被设置为调度计划表的调度计划

![](/images/execute-orderlist.png)

## 在解挂后使用自定义调度计划表
另见：[解挂后列车调度计划选择](../1-列车解挂/1.2-解挂列车操作/#解挂后列车调度计划选择)

## 载具执行自定义调度计划的实际过程
“执行调度计划”调度计划只会将需要执行的调度计划表执行**一遍**，随后会跳回**载具原始调度计划表**，因此，在这个调度计划中，第二条前往车库的调度计划也能被正常执行：

![](/images/execute-orderlist-out.png)

当一个载具已经进入执行某个自定义调度计划表时，选中调度计划的最后一项并点击“退出当前执行”，也可以跳回载具原调度计划：

![](/images/execute-orderlist-exit.png)

如果载具原始调度计划只有一条“执行调度计划”，那么就等效于重复执行自定义调度计划表。

**注意：当一个自定义调度计划表执行完毕后，会跳回载具最原始的调度计划表，而不是上级调度计划表，也就是说，如果一个自定义调度计划表中有“执行调度计划”命令用于执行另一个调度计划表，在另一个调度计划表执行完毕后，将跳回载具原始的调度计划表，而不是这个自定义调度计划表**
